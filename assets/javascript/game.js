// Start game on keypress
    document.onkeyup = function(event){
        var houseNames = ["stark", "arryn", "baratheon", "tully", "greyjoy", "lannister", "tyrell", "martell", "targaryen"];
        var imgArray = ["assets/images/stark.jpg", "assets/images/arryn.jpg", "assets/images/stag.jpg", "assets/images/tully.png", "assets/images/greyjoy.png",
        "assets/images/lion.jpg", "assets/images/tyrell.jpg", "assets/images/martell.jpg", "assets/images/dragon.png"];

        // name variable created, value generated by random name from array
        var answerArray = houseNames[Math.floor(Math.random() * houseNames.length)];

        // picture matched with corresponding name value
        picture = imgArray[houseNames[i]];

        // Set user guess amount (add 3 from original letter count)
        guessesLeft = name.length + 3;
        document.getElementById("guessesLeft").innerHTML = name;

        // Create underscores for each letter of name
        answerArray = [];
        for (var i = 0; i < name.length; i++) {
        answerArray[i] = "_";
        // Placeholders for still missing letters
        document.getElementById("guessbox").innerHTML = answerArray;
        

        // Variable that holds number of remainingLetters to be guessed
        var remainingLetters = name.length;
    
        // Show letters that are correct
        while (remainingLetters > 0){
            // Get Guess
            document.onkeyup = function(event) {
                var userGuess = event.key;
                for (var j = 0; j < name.length; j++){
                if (name[j] === userGuess){
                answerArray[j] = userGuess;
                remainingLetters--;
                document.getElementById("guessbox").innerHTML = remainingLetters;
            }
        }
    } 
};  
// If guess is correct, count as win

// If guess is wrong, add letter to "letters guessed"